<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/coordinator"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <include android:id="@+id/included_layout_toolbar" layout="@layout/layout_toolbar" />

    <FrameLayout
        android:id="@+id/fragment_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true"
        app:layout_behavior="@string/appBarFixScrollingFooterBehavior" />

    <include android:id="@+id/included_layout_default_placeholders" layout="@layout/layout_default_placeholders" />

    <include android:id="@+id/included_layout_progress" layout="@layout/layout_progress" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>